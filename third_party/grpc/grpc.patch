diff --git a/bazel/cc_grpc_library.bzl b/bazel/cc_grpc_library.bzl
index 9f7e21b..164aec9 100644
--- a/bazel/cc_grpc_library.bzl
+++ b/bazel/cc_grpc_library.bzl
@@ -29,6 +29,7 @@ def cc_grpc_library(
         allow_deprecated = False,
         use_external = False,  # @unused
         grpc_only = False,
+        plugin_flags = [],
         **kwargs):
     """Generates C++ grpc classes for services defined in a proto file.
 
@@ -110,6 +111,7 @@ def cc_grpc_library(
             well_known_protos = well_known_protos,
             generate_mocks = generate_mocks,
             allow_deprecated = allow_deprecated,
+            flags = plugin_flags,
             **kwargs
         )
 
diff --git a/bazel/cython_library.bzl b/bazel/cython_library.bzl
index cd2e88d..93bbe61 100644
--- a/bazel/cython_library.bzl
+++ b/bazel/cython_library.bzl
@@ -69,6 +69,8 @@ def pyx_library(name, deps = [], py_deps = [], srcs = [], **kwargs):
 
     shared_objects = []
     defines = kwargs.pop("defines", [])
+    copts = kwargs.pop("copts", [])
+    linkopts = kwargs.pop("linkopts", [])
     for src in pyx_srcs:
         stem = src.split(".")[0]
         shared_object_name = stem + ".so"
@@ -79,12 +81,19 @@ def pyx_library(name, deps = [], py_deps = [], srcs = [], **kwargs):
                 "@rules_python//python/cc:current_py_cc_headers",
             ],
             defines = defines,
+            copts = copts,
+            features = kwargs.get("features", []),
             linkshared = 1,
             linkopts = select({
                 # The "-undefined dynamic_lookup" flag allows the shared library to use symbols
                 # that are not defined at link time but will be resolved at runtime.
                 # This is necessary for Python extensions on macOS to access Python C API symbols.
                 "@platforms//os:macos": ["-undefined", "dynamic_lookup"],
+                "@platforms//os:windows": [
+                    "/NODEFAULTLIB:python3.lib",
+                    "/OPT:REF",
+                    "/OPT:ICF",
+                ],
                 "//conditions:default": [],
             }),
         )
diff --git a/src/core/BUILD b/src/core/BUILD
index b52d18d..262e645 100644
--- a/src/core/BUILD
+++ b/src/core/BUILD
@@ -11470,7 +11470,16 @@ grpc_cc_library(
 
 grpc_cc_library(
     name = "channelz_v2tov1_property_list",
-    srcs = ["channelz/v2tov1/property_list.cc"],
+    srcs = [
+        "channelz/v2tov1/property_list.cc",
+        "ext/upb-gen/google/protobuf/any.upb.h",
+        "ext/upb-gen/google/protobuf/any.upb_minitable.h",
+        "ext/upb-gen/google/protobuf/duration.upb.h",
+        "ext/upb-gen/google/protobuf/duration.upb_minitable.h",
+        "ext/upb-gen/google/protobuf/timestamp.upb.h",
+        "ext/upb-gen/google/protobuf/timestamp.upb_minitable.h",
+        "ext/upb-gen/google/protobuf/empty.upb_minitable.h",
+    ],
     hdrs = ["channelz/v2tov1/property_list.h"],
     external_deps = [
         "absl/strings:string_view",
@@ -11482,6 +11491,7 @@ grpc_cc_library(
         "//:protobuf_any_upb",
         "//:protobuf_duration_upb",
         "//:protobuf_timestamp_upb",
+	"//:protobuf_empty_upb",
     ],
 )
 
diff --git a/src/core/lib/promise/party.h b/src/core/lib/promise/party.h
index a682cb0..9419ab1 100644
--- a/src/core/lib/promise/party.h
+++ b/src/core/lib/promise/party.h
@@ -21,6 +21,7 @@
 #include <stdint.h>
 
 #include <atomic>
+#include <cinttypes>
 #include <limits>
 #include <string>
 #include <utility>
@@ -692,7 +693,7 @@ class Party : public Activity, private Wakeable {
       DebugLocation loc = {}) {
     GRPC_TRACE_LOG(party_state, INFO).AtLocation(loc.file(), loc.line())
         << this << " " << op << " "
-        << absl::StrFormat("%016" PRIx64 " -> %016" PRIx64, prev_state,
+        << absl::StrFormat("%016lx -> %016lx", prev_state,
                            new_state);
   }
 
diff --git a/src/core/resolver/dns/dns_resolver_plugin.cc b/src/core/resolver/dns/dns_resolver_plugin.cc
index 146d23a..6fd4592 100644
--- a/src/core/resolver/dns/dns_resolver_plugin.cc
+++ b/src/core/resolver/dns/dns_resolver_plugin.cc
@@ -48,6 +48,9 @@ void RegisterDnsResolver(CoreConfiguration::Builder* builder) {
     return;
   }
   auto resolver = ConfigVars::Get().DnsResolver();
+  if (resolver.empty()) {
+    resolver = "native";
+  }
   // ---- Ares resolver ----
   if (ShouldUseAresDnsResolver(resolver)) {
     VLOG(2) << "Using ares dns resolver";
diff --git a/src/python/grpcio/grpc/BUILD.bazel b/src/python/grpcio/grpc/BUILD.bazel
index eccd7c5..c3b1f3a 100644
--- a/src/python/grpcio/grpc/BUILD.bazel
+++ b/src/python/grpcio/grpc/BUILD.bazel
@@ -108,9 +108,9 @@ py_library(
 py_library(
     name = "grpcio",
     srcs = ["__init__.py"],
-    data = [
-        "//:grpc",
-    ],
+#    data = [
+#        "//:grpc",
+#    ],
     imports = ["../"],
     deps = [
         ":_observability",
diff --git a/src/python/grpcio/grpc/_cython/BUILD.bazel b/src/python/grpcio/grpc/_cython/BUILD.bazel
index bccc98c..c25f2a0 100644
--- a/src/python/grpcio/grpc/_cython/BUILD.bazel
+++ b/src/python/grpcio/grpc/_cython/BUILD.bazel
@@ -33,6 +33,22 @@ pyx_library(
     ],
     data = [":copy_roots_pem"],
     defines = ["GRPC_PYTHON_BUILD=1"],
+    # -Wno-unused-result is needed because Cython generated code triggers unused result warnings.
+    copts = select({
+        "@platforms//os:windows": [],
+        "//conditions:default": ["-Wno-unused-result"],
+    }),
+    linkopts = select({
+        "@platforms//os:windows": [
+            "/NODEFAULTLIB:python3.lib",
+            "/WX:NO",
+        ],
+        "//conditions:default": [],
+    }),
+    features = select({
+        "@platforms//os:windows": ["-layering_check", "force_no_whole_archive"],
+        "//conditions:default": ["-layering_check"],
+    }),
     deps = [
         "//:grpc",
         "//src/python/grpcio/grpc/_cython/_cygrpc/private_key_signing:private_key_signer_py_wrapper",
diff --git a/src/python/grpcio/grpc/_cython/_cygrpc/private_key_signing/BUILD.bazel b/src/python/grpcio/grpc/_cython/_cygrpc/private_key_signing/BUILD.bazel
index cf24414..2860f36 100644
--- a/src/python/grpcio/grpc/_cython/_cygrpc/private_key_signing/BUILD.bazel
+++ b/src/python/grpcio/grpc/_cython/_cygrpc/private_key_signing/BUILD.bazel
@@ -42,6 +42,5 @@ grpc_cc_library(
         "//:gpr",
         "//:grpc",
         "@rules_python//python/cc:current_py_cc_headers",
-        "@rules_python//python/cc:current_py_cc_libs",
     ],
 )
